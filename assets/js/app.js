document.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{let e=document.querySelector(b.dataset.copy);try{await navigator.clipboard.writeText(e.value||e.textContent);b.textContent='COPIED';setTimeout(()=>b.textContent='COPY',1500)}catch{prompt('Copy:',e.value||e.textContent)}}));document.querySelectorAll('[data-modal]').forEach(b=>b.addEventListener('click',()=>document.querySelector(b.dataset.modal).classList.add('open')));document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>b.closest('.modal').classList.remove('open')));document.querySelectorAll('[data-eye]').forEach(b=>b.addEventListener('click',()=>{let i=document.getElementById(b.dataset.eye);i.type=i.type==='password'?'text':'password'}));
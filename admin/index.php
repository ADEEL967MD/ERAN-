<?php require_once __DIR__.'/includes/auth.php';$page_title='Admin Dashboard';$users=(int)db()->query('SELECT COUNT(*) FROM users WHERE is_admin=0')->fetchColumn();$packages=(int)db()->query("SELECT COUNT(*) FROM packages WHERE status='ACTIVE'")->fetchColumn();$pending=(int)db()->query("SELECT COUNT(*) FROM demo_transactions WHERE status='PENDING'")->fetchColumn();$logs=(int)db()->query('SELECT COUNT(*) FROM activity_logs')->fetchColumn();include __DIR__.'/includes/header.php';?><div class="page-title"><div><h1>Dashboard</h1><p>ERAN+ Admin · MySQL API management</p></div><a class="button button-primary" href="settings.php">GENERAL SETTINGS</a></div><div class="grid"><div class="metric"><p>TOTAL USERS</p><strong><?=number_format($users)?></strong></div><div class="metric"><p>ACTIVE PACKAGES</p><strong><?=number_format($packages)?></strong></div><div class="metric"><p>PENDING REQUESTS</p><strong><?=number_format($pending)?></strong></div><div class="metric"><p>ACTIVITY LOGS</p><strong><?=number_format($logs)?></strong></div></div><section class="panel"><h2>Independent connection</h2><div class="notice">The public panel calls this application through HTTPS REST API only. MySQL credentials stay inside this admin panel and are never sent to public users.</div><div class="form-grid"><div class="field"><label>API BASE URL</label><input class="input" readonly value="<?=e(APP_URL)?>/admin/api"></div><div class="field"><label>PUBLIC ORIGIN</label><input class="input" readonly value="<?=e(PUBLIC_PANEL_ORIGIN)?>"></div></div></section><?php include __DIR__.'/includes/footer.php';?>
